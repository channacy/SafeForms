# Backend environment variables (copy to .env for local development)

# OpenAI API key used by backend RAG answer generation in app/api/services/rag.py
# Obtain from https://platform.openai.com/ and paste your key value below in your local .env (do not commit real keys)
OPENAI_API_KEY=your_api_key_here

# CORS origins allowed by FastAPI (comma-separated). Default covers Next.js dev server.
ALLOWED_ORIGINS=http://localhost:3000

# Optional: if your backend is not running on the default localhost:8000, set and consume accordingly.
# Example: http://127.0.0.1:8000 or http://api.myhost.com
# API_BASE_URL=http://localhost:8000

# Frontend can also read this at build/runtime if you create app/frontend/.env.local instead.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional: List-Unsubscribe headers for emails (recommended if sending non-transactional mail)
# Examples:
# LIST_UNSUBSCRIBE=<mailto:unsubscribe@yourdomain.com>, <https://yourapp.com/unsubscribe?email=%EMAIL%>
# LIST_UNSUBSCRIBE_POST=List-Unsubscribe=One-Click

# =====================
# Email (SMTP) Settings
# =====================
# Configure SMTP to enable the email agent routes in `app/api/routes/email.py`.
# These are typical settings for a transactional email provider.
# Never commit real credentials.

# SMTP server configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
FROM_EMAIL=SafeForms Agent <no-reply@example.com>

# Default recipients (comma-separated). Can be overridden per request body.
# These should be the two stakeholders to receive Progress and Completion emails.
PROGRESS_RECIPIENTS=alice@example.com,bob@example.com
COMPLETION_RECIPIENTS=alice@example.com,bob@example.com
